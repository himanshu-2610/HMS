<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Appointments - Hospital Management System</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        /* ===== MAIN CONTENT ===== */
        .main-content {
            margin-left: 280px;
            padding: 2rem;
            min-height: 100vh;
            transition: var(--transition);
            background-color: #f5f7fa;
        }

        @media (max-width: 992px) {
            .main-content {
                margin-left: 0;
                padding: 1.5rem;
            }
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        /* ===== PAGE HEADER ===== */
        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .page-title {
            font-size: 1.75rem;
            color: var(--primary-dark);
            margin: 0;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        /* ===== CARD STYLES ===== */
        .card {
            background-color: var(--white-color);
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            margin-bottom: 2rem;
            overflow: hidden;
            border: 1px solid rgba(0, 0, 0, 0.05);
        }

        .card-header {
            padding: 1.25rem 1.5rem;
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
            background-color: var(--white-color);
        }

        .card-title {
            font-size: 1.25rem;
            color: var(--primary-dark);
            margin: 0;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .card-body {
            padding: 1.5rem;
        }

        /* ===== TABLE STYLES ===== */
        .table-responsive {
            overflow-x: auto;
            -webkit-overflow-scrolling: touch;
            border-radius: var(--border-radius);
        }

        .table {
            width: 100%;
            border-collapse: collapse;
            font-size: 0.95rem;
        }

        .table th {
            background-color: var(--light-color);
            padding: 1rem;
            text-align: left;
            font-weight: 600;
            color: var(--dark-color);
            white-space: nowrap;
        }

        .table td {
            padding: 1rem;
            border-bottom: 1px solid rgba(0, 0, 0, 0.05);
            vertical-align: middle;
        }

        .table tr:last-child td {
            border-bottom: none;
        }

        .table tr:hover td {
            background-color: rgba(67, 97, 238, 0.03);
        }

        /* ===== BADGE STYLES ===== */
        .badge {
            display: inline-block;
            padding: 0.35rem 0.75rem;
            border-radius: 50px;
            font-size: 0.8rem;
            font-weight: 500;
        }

        .badge-primary {
            background-color: rgba(67, 97, 238, 0.1);
            color: var(--primary-color);
        }

        .badge-success {
            background-color: rgba(76, 175, 80, 0.1);
            color: var(--success-color);
        }

        .badge-danger {
            background-color: rgba(239, 35, 60, 0.1);
            color: var(--danger-color);
        }

        .badge-warning {
            background-color: rgba(248, 150, 30, 0.1);
            color: var(--warning-color);
        }

        /* ===== BUTTON STYLES ===== */
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 0.75rem 1.5rem;
            border-radius: var(--border-radius);
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
            transition: var(--transition);
            border: none;
            gap: 0.5rem;
        }

        .btn-primary {
            background-color: var(--primary-color);
            color: var(--white-color);
        }

        .btn-primary:hover {
            background-color: var(--primary-dark);
            transform: translateY(-2px);
        }

        .btn-danger {
            background-color: var(--danger-color);
            color: var(--white-color);
        }

        .btn-danger:hover {
            background-color: #d11a2d;
            transform: translateY(-2px);
        }

        .btn-sm {
            padding: 0.5rem 1rem;
            font-size: 0.85rem;
        }

        /* ===== TEXT UTILITIES ===== */
        .text-center {
            text-align: center;
        }

        /* ===== EMPTY STATE ===== */
        .empty-state {
            text-align: center;
            padding: 3rem;
            color: var(--gray-color);
        }

        .empty-state i {
            font-size: 3rem;
            margin-bottom: 1rem;
            color: var(--primary-light);
        }

        /* ===== RESPONSIVE ADJUSTMENTS ===== */
        @media (max-width: 768px) {
            .card-header,
            .card-body {
                padding: 1.25rem;
            }

            .table th,
            .table td {
                padding: 0.75rem;
            }

            .page-header {
                flex-direction: column;
                align-items: flex-start;
            }
        }

        @media (max-width: 576px) {
            .main-content {
                padding: 1rem;
            }

            .page-title {
                font-size: 1.5rem;
            }

            .card-title {
                font-size: 1.1rem;
            }

            .btn {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>
    <%- include('../partials/sidebar', {activePage: 'appointments', user: user}) %>

    <main class="main-content">
        <div class="container">
            <div class="page-header">
                <h1 class="page-title">
                    <i class="fas fa-calendar-check"></i> My Appointments
                </h1>
                <a href="/patient/appointments/book" class="btn btn-primary">
                    <i class="fas fa-plus"></i> Book New Appointment
                </a>
            </div>

            <div class="card">
                <div class="card-header">
                    <h2 class="card-title">
                        <i class="fas fa-list"></i> All Appointments
                    </h2>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th><i class="fas fa-hashtag"></i> ID</th>
                                    <th><i class="fas fa-user-md"></i> Doctor</th>
                                    <th><i class="fas fa-stethoscope"></i> Specialization</th>
                                    <th><i class="fas fa-calendar-day"></i> Date</th>
                                    <th><i class="fas fa-clock"></i> Time</th>
                                    <th><i class="fas fa-circle"></i> Status</th>
                                    <th><i class="fas fa-comment-medical"></i> Reason</th>
                                    <th><i class="fas fa-cog"></i> Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if (appointments.length === 0) { %>
                                    <tr>
                                        <td colspan="8" class="empty-state">
                                            <i class="fas fa-calendar-times"></i>
                                            <h3>No Appointments Found</h3>
                                            <p>You don't have any appointments scheduled yet.</p>
                                        </td>
                                    </tr>
                                <% } else { %>
                                    <% appointments.forEach(appointment => { %>
                                        <tr>
                                            <td>#<%= appointment.appointment_id %></td>
                                            <td>
                                                <div style="display: flex; align-items: center; gap: 0.5rem;">
                                                    <img src="<%= appointment.doctor_avatar || 'https://ui-avatars.com/api/?name=' + appointment.doctor_name + '&background=random' %>" 
                                                         alt="<%= appointment.doctor_name %>" 
                                                         style="width: 32px; height: 32px; border-radius: 50%; object-fit: cover;">
                                                    <%= appointment.doctor_name %>
                                                </div>
                                            </td>
                                            <td><%= appointment.specialization %></td>
                                            <td><%= new Date(appointment.appointment_date).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %></td>
                                            <td><%= appointment.appointment_time %></td>
                                            <td>
                                                <span class="badge <%= appointment.status === 'Scheduled' ? 'badge-primary' : appointment.status === 'Completed' ? 'badge-success' : appointment.status === 'Cancelled' ? 'badge-danger' : 'badge-warning' %>">
                                                    <i class="fas <%= appointment.status === 'Scheduled' ? 'fa-clock' : appointment.status === 'Completed' ? 'fa-check' : appointment.status === 'Cancelled' ? 'fa-times' : 'fa-exclamation' %>"></i>
                                                    <%= appointment.status %>
                                                </span>
                                            </td>
                                            <td><%= appointment.reason || 'N/A' %></td>
                                            <td>
                                                <% if (appointment.status === 'Scheduled') { %>
                                                    <div class="action-buttons" style="display: flex; gap: 0.5rem;">
                                                        <form action="/patient/appointment/cancel/<%= appointment.appointment_id %>" method="POST" style="display: inline;">
                                                            <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure you want to cancel this appointment?')">
                                                                <i class="fas fa-times"></i> Cancel
                                                            </button>
                                                        </form>
        
                                                    </div>
                                                <% }  else { %>
                                                    <span class="text-muted">No actions</span>
                                                <% } %>
                                            </td>
                                        </tr>
                                    <% }) %>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script src="/js/main.js"></script>
    <script>
        // Add confirmation for cancellation
        document.querySelectorAll('form[action*="cancel"]').forEach(form => {
            form.addEventListener('submit', function(e) {
                if (!confirm('Are you sure you want to cancel this appointment?')) {
                    e.preventDefault();
                }
            });
        });

        // Highlight upcoming appointments
        document.addEventListener('DOMContentLoaded', function() {
            const today = new Date();
            today.setHours(0, 0, 0, 0);
            
            document.querySelectorAll('tbody tr').forEach(row => {
                const dateCell = row.querySelector('td:nth-child(4)');
                if (dateCell) {
                    const dateStr = dateCell.textContent.trim();
                    const appointmentDate = new Date(dateStr);
                    
                    if (appointmentDate >= today) {
                        row.style.backgroundColor = 'rgba(67, 97, 238, 0.05)';
                        row.style.borderLeft = '3px solid var(--primary-color)';
                    }
                }
            });
        });
    </script>
</body>
</html>