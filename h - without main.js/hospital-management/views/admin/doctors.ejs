<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Doctors - Hospital Management System</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        /* ===== MAIN CONTENT STRUCTURE ===== */
        .main-content {
            margin-left: 280px; /* Matches sidebar width */
            padding: 2rem;
            min-height: 100vh;
            transition: var(--transition);
            background-color: var(--white-color);
        }
    
        @media (max-width: 992px) {
            .main-content {
                margin-left: 0;
                padding: 1.5rem;
            }
        }
    
        /* ===== PAGE HEADER ===== */
        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            flex-wrap: wrap;
            gap: 1rem;
        }
    
        .page-title {
            font-size: 1.75rem;
            color: var(--primary-dark);
            margin: 0;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }
    
        /* ===== CARD COMPONENTS ===== */
        .card {
            background-color: var(--white-color);
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            margin-bottom: 2rem;
            overflow: hidden;
            border: 1px solid rgba(0, 0, 0, 0.05);
        }
    
        .card-header {
            padding: 1.25rem 1.5rem;
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
            background-color: var(--white-color);
        }
    
        .card-title {
            font-size: 1.25rem;
            color: var(--primary-dark);
            margin: 0;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
    
        .card-body {
            padding: 1.5rem;
        }
    
        /* ===== DATA TABLE STYLES ===== */
        .table-responsive {
            overflow-x: auto;
            -webkit-overflow-scrolling: touch;
            border-radius: var(--border-radius);
        }
    
        .table {
            width: 100%;
            border-collapse: collapse;
            font-size: 0.95rem;
        }
    
        .table th {
            background-color: var(--light-color);
            padding: 1rem;
            text-align: left;
            font-weight: 600;
            color: var(--dark-color);
            white-space: nowrap;
        }
    
        .table td {
            padding: 1rem;
            border-bottom: 1px solid rgba(0, 0, 0, 0.05);
            vertical-align: middle;
        }
    
        .table tr:last-child td {
            border-bottom: none;
        }
    
        .table tr:hover td {
            background-color: rgba(67, 97, 238, 0.03);
        }
    
        /* ===== STATUS BADGES ===== */
        .badge {
            display: inline-block;
            padding: 0.35rem 0.75rem;
            border-radius: 50px;
            font-size: 0.8rem;
            font-weight: 500;
        }
    
        .badge-success {
            background-color: rgba(76, 175, 80, 0.1);
            color: var(--success-color);
        }
    
        .badge-danger {
            background-color: rgba(239, 35, 60, 0.1);
            color: var(--danger-color);
        }
    
        /* ===== ACTION BUTTONS ===== */
        .action-buttons {
            display: flex;
            gap: 0.5rem;
        }
    
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 0.5rem 1rem;
            border-radius: var(--border-radius);
            font-size: 0.9rem;
            font-weight: 500;
            cursor: pointer;
            transition: var(--transition);
            border: none;
            gap: 0.5rem;
        }
    
        .btn-sm {
            padding: 0.4rem 0.8rem;
            font-size: 0.85rem;
        }
    
        .btn-primary {
            background-color: var(--primary-color);
            color: var(--white-color);
        }
    
        .btn-primary:hover {
            background-color: var(--primary-dark);
            transform: translateY(-2px);
        }
    
        .btn-success {
            background-color: var(--success-color);
            color: var(--white-color);
        }
    
        .btn-warning {
            background-color: var(--warning-color);
            color: var(--white-color);
        }
    
        .btn-danger {
            background-color: var(--danger-color);
            color: var(--white-color);
        }
    
        /* ===== EMPTY STATE ===== */
        .empty-state {
            text-align: center;
            padding: 3rem;
            color: var(--gray-color);
        }
    
        .empty-state i {
            font-size: 3rem;
            margin-bottom: 1rem;
            color: var(--primary-light);
        }
    
        /* ===== RESPONSIVE ADJUSTMENTS ===== */
        @media (max-width: 768px) {
            .card-header,
            .card-body {
                padding: 1.25rem;
            }
    
            .table th,
            .table td {
                padding: 0.75rem;
            }
    
            .action-buttons {
                flex-direction: column;
                gap: 0.5rem;
            }
    
            .btn {
                width: 100%;
            }
        }
    
        @media (max-width: 576px) {
            .main-content {
                padding: 1rem;
            }
    
            .page-title {
                font-size: 1.5rem;
            }
    
            .card-title {
                font-size: 1.1rem;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>

    <%- include('../partials/sidebar', {activePage: 'doctors', user: user}) %>

    <main class="main-content">
        
<div class="page-header">
    <h1 class="page-title">
        <i class="fas fa-user-md"></i> Doctors
    </h1>
    <a href="/register/doctor" class="btn btn-primary">
        <i class="fas fa-plus"></i> Add New Doctor
    </a>
</div>

<div class="card">
    <div class="card-header">
        <h2 class="card-title">
            <i class="fas fa-list"></i> All Doctors
        </h2>
    </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Name</th>
                                <th>Specialization</th>
                                <th>Email</th>
                                <th>Phone</th>
                                <th>Status</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% doctors.forEach(doctor => { %>
                                <tr>
                                    <td><%= doctor.doctor_id %></td>
                                    <td><%= doctor.full_name %></td>
                                    <td><%= doctor.specialization %></td>
                                    <td><%= doctor.email %></td>
                                    <td><%= doctor.phone || 'N/A' %></td>
                                    <td>
                                        <% if (doctor.is_active) { %>
                                            <span class="badge badge-success">Active</span>
                                        <% } else { %>
                                            <span class="badge badge-danger">Inactive</span>
                                        <% } %>
                                    </td>
                                    <td>
                                        <div class="action-buttons">
                                            <a href="/admin/doctor/<%= doctor.doctor_id %>" class="btn btn-sm btn-primary">
                                                <i class="fas fa-eye"></i> View
                                            </a>
                                            <form action="/admin/doctor/status/<%= doctor.doctor_id %>" method="POST" style="display: inline;">
                                                <button type="submit" class="btn btn-sm <%= doctor.is_active ? 'btn-warning' : 'btn-success' %>">
                                                    <%= doctor.is_active ? 'Deactivate' : 'Activate' %>
                                                </button>
                                            </form>
                                            <form action="/admin/doctor/delete/<%= doctor.doctor_id %>" method="POST" style="display: inline;">
                                                <button type="submit" class="btn btn-sm btn-danger">
                                                    <i class="fas fa-trash-alt"></i> Delete
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </main>

    <script src="/js/main.js"></script>
    <script>
        // Simple confirmation for delete actions
        document.querySelectorAll('form[action*="delete"]').forEach(form => {
            form.addEventListener('submit', function(e) {
                if (!confirm('Are you sure you want to delete this doctor?')) {
                    e.preventDefault();
                }
            });
        });
    </script>
</body>
</html>