<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bed Management - Hospital Management System</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        /* ============== BED MANAGEMENT PAGE STYLES ============== */
        :root {
            --primary-color: #4361ee;
            --primary-dark: #3a0ca3;
            --primary-light: #4895ef;
            --success-color: #4caf50;
            --danger-color: #ef233c;
            --warning-color: #f8961e;
            --white-color: #ffffff;
            --light-color: #f8f9fa;
            --dark-color: #212529;
            --gray-color: #6c757d;
            --border-radius: 10px;
            --shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s ease;
        }

        body {
            font-family: 'Poppins', sans-serif;
            color: var(--dark-color);
            background-color: #f5f7fa;
            margin: 0;
            padding: 0;
        }

        .main-content {
            margin-left: 250px;
            padding: 2rem;
            transition: var(--transition);
        }

        @media (max-width: 992px) {
            .main-content {
                margin-left: 0;
                padding: 1.5rem;
            }
        }

        /* Page Header */
        .page-header {
            margin-bottom: 2rem;
        }

        .page-title {
            font-size: 2rem;
            color: var(--primary-dark);
            margin: 0;
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        /* Card Styles */
        .card {
            background-color: var(--white-color);
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            margin-bottom: 1.5rem;
            overflow: hidden;
            border: 1px solid rgba(0, 0, 0, 0.05);
        }

        .mt-20 {
            margin-top: 1.25rem;
        }

        .card-header {
            padding: 1.25rem 1.5rem;
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
            background-color: var(--light-color);
        }

        .card-title {
            font-size: 1.5rem;
            color: var(--primary-dark);
            margin: 0;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .card-body {
            padding: 1.5rem;
        }

        /* Form Styles */
        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: var(--dark-color);
        }

        .form-control {
            width: 100%;
            padding: 0.8rem 1rem;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-size: 1rem;
            transition: var(--transition);
            font-family: 'Poppins', sans-serif;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(67, 97, 238, 0.2);
        }

        /* Table Styles */
        .table-responsive {
            overflow-x: auto;
            -webkit-overflow-scrolling: touch;
            border-radius: var(--border-radius);
        }

        .table {
            width: 100%;
            border-collapse: collapse;
            font-size: 0.95rem;
        }

        .table th {
            background-color: var(--light-color);
            color: var(--dark-color);
            font-weight: 600;
            text-align: left;
            padding: 1rem;
            white-space: nowrap;
        }

        .table td {
            padding: 1rem;
            border-bottom: 1px solid rgba(0, 0, 0, 0.05);
            vertical-align: middle;
        }

        .table tr:last-child td {
            border-bottom: none;
        }

        .table tr:hover td {
            background-color: rgba(67, 97, 238, 0.03);
        }

        /* Badge Styles */
        .badge {
            display: inline-block;
            padding: 0.35em 0.65em;
            font-size: 0.75em;
            font-weight: 700;
            line-height: 1;
            text-align: center;
            white-space: nowrap;
            vertical-align: baseline;
            border-radius: 50px;
        }

        .badge-success {
            background-color: #dcfce7;
            color: #166534;
        }

        .badge-primary {
            background-color: #dbeafe;
            color: #1e40af;
        }

        .badge-warning {
            background-color: #fef3c7;
            color: #92400e;
        }

        /* Button Styles */
        .btn {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.8rem 1.5rem;
            border-radius: 8px;
            font-weight: 500;
            cursor: pointer;
            transition: var(--transition);
            text-decoration: none;
            border: none;
        }

        .btn-sm {
            padding: 0.5rem 1rem;
            font-size: 0.85rem;
        }

        .btn-primary {
            background-color: var(--primary-color);
            color: white;
        }

        .btn-primary:hover {
            background-color: var(--primary-dark);
            transform: translateY(-2px);
        }

        .btn-danger {
            background-color: var(--danger-color);
            color: white;
        }

        .btn-danger:hover {
            background-color: #dc2626;
            transform: translateY(-2px);
        }

        /* Inline Forms */
        .inline-form {
            display: flex;
            gap: 1rem;
            align-items: flex-end;
        }

        .inline-form .form-group {
            margin-bottom: 0;
            flex: 1;
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 3rem;
            color: var(--gray-color);
        }

        .empty-state i {
            font-size: 3rem;
            margin-bottom: 1rem;
            color: var(--primary-light);
        }

        /* Responsive Adjustments */
        @media (max-width: 768px) {
            .card-header,
            .card-body {
                padding: 1.25rem;
            }
            
            .table th, 
            .table td {
                padding: 0.75rem 0.5rem;
            }
            
            .inline-form {
                flex-direction: column;
                align-items: stretch;
            }
        }

        @media (max-width: 576px) {
            .main-content {
                padding: 1rem;
            }
            
            .page-title {
                font-size: 1.75rem;
            }
            
            .card-title {
                font-size: 1.25rem;
            }
            
            .btn {
                padding: 0.7rem 1.2rem;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>
    <%- include('../partials/sidebar', {activePage: 'beds', user: user}) %>

    <main class="main-content">
        <div class="container">
            <div class="page-header">
                <h1 class="page-title"><i class="fas fa-bed"></i> Bed Management</h1>
            </div>

            <!-- Add New Bed Card -->
            <div class="card">
                <div class="card-header">
                    <h2 class="card-title"><i class="fas fa-plus-circle"></i> Add New Bed</h2>
                </div>
                <div class="card-body">
                    <form action="/admin/beds" method="POST">
                        <div class="form-group">
                            <label for="bed_number"><i class="fas fa-hashtag"></i> Bed Number</label>
                            <input type="text" id="bed_number" name="bed_number" class="form-control" required>
                        </div>
                        <div class="form-group">
                            <label for="ward"><i class="fas fa-procedures"></i> Ward</label>
                            <input type="text" id="ward" name="ward" class="form-control" required>
                        </div>
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save"></i> Add Bed
                        </button>
                    </form>
                </div>
            </div>

            <!-- All Beds Card -->
            <div class="card mt-20">
                <div class="card-header">
                    <h2 class="card-title"><i class="fas fa-list"></i> All Beds</h2>
                </div>
                <div class="card-body">
                    <% if (beds.length > 0) { %>
                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Bed Number</th>
                                        <th>Ward</th>
                                        <th>Status</th>
                                        <th>Patient</th>
                                        <th>Admission Date</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% beds.forEach(bed => { %>
                                        <tr>
                                            <td><strong><%= bed.bed_number %></strong></td>
                                            <td><%= bed.ward %></td>
                                            <td>
                                                <span class="badge <%= bed.status === 'Available' ? 'badge-success' : bed.status === 'Occupied' ? 'badge-primary' : 'badge-warning' %>">
                                                    <%= bed.status %>
                                                </span>
                                            </td>
                                            <td><%= bed.patient_name || 'N/A' %></td>
                                            <td><%= bed.admission_date ? new Date(bed.admission_date).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) : 'N/A' %></td>
                                            <td>
                                                <% if (bed.status === 'Available') { %>
                                                    <form action="/admin/bed/assign/<%= bed.bed_id %>" method="POST" class="inline-form">
                                                        <div class="form-group">
                                                            <label for="patient_id_<%= bed.bed_id %>"><i class="fas fa-user"></i> Patient ID</label>
                                                            <input type="number" id="patient_id_<%= bed.bed_id %>" name="patient_id" class="form-control" required>
                                                        </div>
                                                        <div class="form-group">
                                                            <label for="admission_date_<%= bed.bed_id %>"><i class="fas fa-calendar-day"></i> Admission Date</label>
                                                            <input type="date" id="admission_date_<%= bed.bed_id %>" name="admission_date" class="form-control" required>
                                                        </div>
                                                        <button type="submit" class="btn btn-sm btn-primary">
                                                            <i class="fas fa-user-plus"></i> Assign
                                                        </button>
                                                    </form>
                                                <% } else if (bed.status === 'Occupied') { %>
                                                    <form action="/admin/bed/discharge/<%= bed.bed_id %>" method="POST" class="inline-form">
                                                        <div class="form-group">
                                                            <label for="discharge_date_<%= bed.bed_id %>"><i class="fas fa-calendar-times"></i> Discharge Date</label>
                                                            <input type="date" id="discharge_date_<%= bed.bed_id %>" name="discharge_date" class="form-control" required>
                                                        </div>
                                                        <button type="submit" class="btn btn-sm btn-danger">
                                                            <i class="fas fa-user-minus"></i> Discharge
                                                        </button>
                                                    </form>
                                                <% } %>
                                            </td>
                                        </tr>
                                    <% }) %>
                                </tbody>
                            </table>
                        </div>
                    <% } else { %>
                        <div class="empty-state">
                            <i class="fas fa-bed"></i>
                            <h3>No Beds Found</h3>
                            <p>There are no beds registered in the system yet.</p>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>
    </main>

    <script>
        // Set default dates for admission and discharge forms
        document.querySelectorAll('input[type="date"]').forEach(input => {
            if (!input.value) {
                input.valueAsDate = new Date();
            }
        });
    </script>

    <script src="/js/main.js"></script>
</body>
</html>